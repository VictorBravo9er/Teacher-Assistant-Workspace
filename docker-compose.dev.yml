services:
  backend:
    image: ta-backend-dev
    container_name: ta-backend-dev
    command: sh -c "exec /app/.venv/bin/uvicorn src.main:app --host 0.0.0.0 --port $${BACKEND_PORT} --reload --reload-dir src"
    ports:
      - "${BACKEND_PORT:-8001}:${BACKEND_PORT:-8001}"
    volumes:
      - ./backend/src:/app/src:ro

  frontend:
    image: ta-frontend-dev
    container_name: ta-frontend-dev
    build:
      target: dev
    command: sh -c "exec npm run dev -- --host 0.0.0.0 --port $${FRONTEND_PORT}"
    ports:
      - "${HOST_PORT:-8000}:${FRONTEND_PORT:-8000}"
    volumes:
      - ./frontend/src:/app/src:ro
      - ./frontend/public:/app/public:ro
      - ./frontend/index.html:/app/index.html:ro
      - ./frontend/tsconfig.json:/app/tsconfig.json:ro
      - ./frontend/vite.config.ts:/app/vite.config.ts:ro
